---
import "../styles/global.css";
import {
  agentSuggestions,
  audience,
  profile,
  projects,
  proof,
  valueProps,
} from "@/data/portfolio";

const title = `${profile.name} - ${profile.role}`;
const description =
  "Applied AI and backend engineer building LLM apps, FastAPI backends, and full-stack MVPs.";
const contactLinks = [
  { label: "Email", icon: "mail", href: `mailto:${profile.email}` },
  ...profile.socials
    .filter((social) => ["GitHub", "LinkedIn", "X"].includes(social.label))
    .map((social) => ({
      ...social,
      icon: social.label === "GitHub" ? "github" : social.label === "LinkedIn" ? "linkedin" : "x",
    })),
];
const stackKeywords = ["Python","Express", "FastAPI", "LLMs", "Observability","Evals"];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta name="theme-color" content="#f6efe4" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="canonical" href="https://byanujpatel.com/" />
    <title>{title}</title>
  </head>
  <body>
    <div class="page-shell">
      <header class="topbar" aria-label="Portfolio header">
        <a class="brand" href="/" aria-label={`${profile.name} home`}>
          <span class="brand-mark">{profile.initials}</span>
          <span class="brand-name-full">{profile.name}</span>
          <span class="brand-name-short" aria-hidden="true">Anuj</span>
        </a>
        <div class="status-pill">
          <span></span>
          {profile.status}
        </div>
        <nav class="quick-links" aria-label="Contact links">
          {contactLinks.map((link) => (
            <a
              href={link.href}
              target={link.label === "Email" ? undefined : "_blank"}
              rel="noreferrer"
              aria-label={link.label}
              title={link.label}
            >
              {link.icon === "mail" && (
                <svg viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M4 6h16v12H4z" />
                  <path d="m4 7 8 6 8-6" />
                </svg>
              )}
              {link.icon === "github" && (
                <svg viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M12 2a10 10 0 0 0-3.16 19.49c.5.09.68-.22.68-.48v-1.7c-2.78.6-3.37-1.18-3.37-1.18-.46-1.15-1.11-1.46-1.11-1.46-.9-.62.07-.6.07-.6 1 .07 1.53 1.03 1.53 1.03.89 1.52 2.34 1.08 2.91.83.09-.64.35-1.08.63-1.33-2.22-.25-4.55-1.11-4.55-4.94 0-1.09.39-1.98 1.03-2.68-.1-.25-.45-1.27.1-2.65 0 0 .84-.27 2.75 1.02A9.55 9.55 0 0 1 12 6.01c.85 0 1.71.11 2.51.34 1.91-1.29 2.75-1.02 2.75-1.02.55 1.38.2 2.4.1 2.65.64.7 1.03 1.59 1.03 2.68 0 3.84-2.34 4.68-4.57 4.93.36.31.68.92.68 1.86v2.76c0 .27.18.58.69.48A10 10 0 0 0 12 2z" />
                </svg>
              )}
              {link.icon === "linkedin" && (
                <svg viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M5.2 8.9h3.2v10.3H5.2z" />
                  <path d="M6.8 7.5a1.85 1.85 0 1 1 0-3.7 1.85 1.85 0 0 1 0 3.7z" />
                  <path d="M10.5 8.9h3.1v1.4h.04c.43-.81 1.47-1.67 3.03-1.67 3.24 0 3.84 2.13 3.84 4.9v5.67h-3.2v-5.02c0-1.2-.02-2.74-1.67-2.74-1.67 0-1.93 1.3-1.93 2.65v5.11h-3.2z" />
                </svg>
              )}
              {link.icon === "x" && (
                <svg viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M4 4h4.9l3.7 5.1L17 4h3.1l-6 7 6.5 9H15.7l-4.1-5.6L6.8 20H3.7l6.5-7.5zM7.6 6.1l9.2 11.8h1.1L8.7 6.1z" />
                </svg>
              )}
            </a>
          ))}
        </nav>
      </header>

      <main class="portfolio-card" aria-labelledby="hero-title">
        <section class="identity-panel">
          <p class="eyebrow">{profile.role}</p>
          <h1 id="hero-title" aria-label="I build AI products that ship.">
            I build AI products <span>that ship.</span>
          </h1>
          <p class="hero-copy">{profile.subhead}</p>

          <div class="audience-row" aria-label="Target audience">
            {audience.map((item) => <span>{item}</span>)}
            <a class="journey-badge" href="/100-days">100 Days</a>
          </div>

          <div class="mini-proof" aria-label="Proof signals">
            {proof.map((item) => (
              <div>
                <span></span>
                <p>{item}</p>
              </div>
            ))}
          </div>
        </section>

        <section class="work-panel" aria-label="Featured work">
          <div class="panel-heading">
            <span>Proof</span>
            <strong>3 projects</strong>
          </div>

          <div class="project-stack">
            {projects.map((project) => (
              <article class="project-card">
                <div class="project-topline">
                  <span>{project.type}</span>
                  <strong>{project.signal}</strong>
                </div>
                <h2>{project.name}</h2>
                <p>{project.solution}</p>
                <div class="stack-line">
                  {project.stack.slice(0, 4).map((tool) => <span>{tool}</span>)}
                </div>
                <div class="project-links">
                  {project.links.map((link) => (
                    <a href={link.href} target="_blank" rel="noreferrer">{link.label}</a>
                  ))}
                </div>
              </article>
            ))}
          </div>
        </section>

        <aside class="signal-panel" aria-label="Why work with Anuj">
          <div class="value-strip">
            {valueProps.map((item) => (
              <article>
                <span>{item.label}</span>
                <p>{item.text}</p>

              </article>
            ))}
          </div>

          <div class="stack-card">
            <span>Works with</span>
            <p>{stackKeywords.join(" / ")}</p>
          </div>

          <section id="agent" class="agent-panel" aria-label="Portfolio agent">
            <div class="agent-head">
              <span>Ask the agent</span>
              <strong>&lt;1s answers</strong>
            </div>
            <div id="chat-log" class="chat-log" aria-live="polite">
              <strong>Try:</strong> Why hire Anuj?
            </div>
            <form id="chat-form" class="chat-form">
              <input
                id="chat-input"
                name="question"
                autocomplete="off"
                maxlength="600"
                placeholder="Ask fit / stack / projects..."
                required
              />
              <button type="submit">Ask</button>
            </form>
            <div class="suggestions" aria-label="Suggested questions">
              {agentSuggestions.map((question) => (
                <button type="button" data-question={question}>{question}</button>
              ))}
            </div>
          </section>
        </aside>
      </main>
    </div>

    <script>
      const form = document.querySelector("#chat-form");
      const input = document.querySelector("#chat-input");
      const log = document.querySelector("#chat-log");
      const suggestionButtons = document.querySelectorAll("[data-question]");

      function setLog(html: string) {
        if (log) log.innerHTML = html;
      }

      async function ask(question: string) {
        const cleanQuestion = question.trim();
        if (!cleanQuestion) return;

        setLog(`<strong>You:</strong> ${cleanQuestion}<br><br>Thinking...`);

        try {
          const response = await fetch("/api/ask", {
            method: "POST",
            headers: { "content-type": "application/json" },
            body: JSON.stringify({ question: cleanQuestion }),
          });
          const data = await response.json();

          if (!response.ok) {
            throw new Error(data?.error || "The agent could not answer.");
          }

          setLog(`<strong>Agent:</strong> ${data.answer}`);
        } catch (error) {
          const message = error instanceof Error ? error.message : "Something went wrong.";
          setLog(`<strong>Agent:</strong> ${message}`);
        }
      }

      form?.addEventListener("submit", (event) => {
        event.preventDefault();
        if (!(input instanceof HTMLInputElement)) return;
        ask(input.value);
      });

      suggestionButtons.forEach((button) => {
        button.addEventListener("click", () => {
          const question = button.getAttribute("data-question") || "";
          if (input instanceof HTMLInputElement) input.value = question;
          ask(question);
        });
      });
    </script>
  </body>
</html>
